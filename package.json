{
  "private": true,
  "workspaces": [
    "mobile",
    "server"
  ],
  "scripts": {
    "build": "npm --workspace mobile run build",
    "postinstall": "npm --workspace server run postinstall"
  }
}
